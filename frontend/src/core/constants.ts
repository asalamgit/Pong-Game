// REGEX
// this regex is linked with the backend
export const PASSWORD_REGEX =
  /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^*-]).{8,}$/;

export const TWO_FA_CODE_REGEX = /^[0-9]{1}$/;
